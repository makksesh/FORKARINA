@model IEnumerable<LibApp.Models.Author>

@{
    ViewData["Title"] = "Index";
}

@if (ViewBag.ErrorMessage != null)
{
    <div class="alert alert-danger">
        @ViewBag.ErrorMessage
    </div>
}

<h1>Авторы</h1>

<p>
    <a asp-action="Create">Добавить</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AuthorId">Изменить</a> |
                <a asp-action="Details" asp-route-id="@item.AuthorId">Подробнее</a> |
                <a asp-action="Delete" asp-route-id="@item.AuthorId">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
